# Billiard

Минимальный billiard MVP на Pygame.

## Запуск

```powershell
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

Если `python` указывает не на виртуальное окружение, запускай так:

```powershell
venv\Scripts\python.exe main.py
```
